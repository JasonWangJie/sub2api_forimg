# Sub2API Fork 开发台账

## 2026-09-21 本地交接文档提交并合并 origin/main

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | 合并后完整 HEAD `d48b1324588fcc87c104b528cfca71b5b723a9e2`；`git describe --tags --always --dirty`=`v0.1.173.50-3-gd48b132`；VERSION=`0.1.173.50`；`main...origin/main [ahead 2]`（本轮交接同步提交前）。 |
| 变更范围 | 提交本地未入库的 OVH TCP/sysctl 交接文档（`3d18116`）；合并远程 `defccfa`/`7bf84c9`/`68be72e`（含 `v0.1.173.50` Gemini 错误诊断与 newapi 迁移文档）。三份记录自动合并无冲突。 |
| 验证命令/结果 | `git pull origin main` 成功；`git diff --check` 通过。本轮未运行业务测试、未部署或重启生产。 |
| 未完成项 | 本地领先提交尚未 `git push`；生产未部署 `v0.1.173.50`；OVH 手机 5G 复测与 OVH Mitigation 控制台确认仍待用户侧完成。 |

## 2026-09-20 异步 Gemini 原始错误持久化与 611–613 分类闭环

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | `main`；完整 HEAD `defccfacba610450ac5f9fd06e0f54c2f25fbb7a`；`git describe --tags --always --dirty`=`v0.1.173.49-1-gdefccfa-dirty`；`backend/cmd/server/VERSION`=`0.1.173.49`。未提交/tag；保留开始前的三份记录修改、两份 `wiki-new/` 修改和四份未跟踪迁移文档。 |
| 后端变更 | `gemini_async_image_errors.go` 新增异步专用安全诊断载体，提取并脱敏/限长原始 `error.message`、provider code 与 request ID；`gemini_chat_completions_compat_service.go` 仅在 Worker 私有异步上下文返回详细错误，同步请求继续使用通用映射。普通映射错误与 `Invalid request` 换号耗尽都会把详细 message 交给 Worker，最终 `error_message` 可进入既有 611–613 分类。 |
| 持久化与展示 | `AsyncImageAccountAttempt` 增加 `provider_error_code`，沿用 `account_attempts` JSONB；request ID 由账号尝试即时持久化并继续提升到 `async_image_tasks.upstream_request_id`，不新增迁移。管理员详情 API/页面展示 provider code，用户视图保持隔离；失败 message 与 code 在写入前脱敏和限制长度。 |
| 测试覆盖 | 新增异步详细错误、同步通用错误隔离、普通/换号耗尽、诊断脱敏、账号尝试 JSON、管理员字段隔离和 611/612/613 链路测试。定向命令 `go test ./internal/service -run 'TestGemini(ForwardAsChatCompletionsAsync|ForwardAsChatCompletionsSync|AsyncAccountFailover400|AsyncUpstreamDiagnostics|NewGeminiAsyncAccountFailover400)|TestAsyncImageAccountAttemptJSONPreservesProviderDiagnostics' -count=1`、`go test ./internal/handler -run 'TestFormatAsyncImageUpstreamFailureUsesChinesePrefix|TestHandleCCFailoverExhaustedAsyncGeminiPreservesProviderDiagnostics|TestAsyncImageFailedAccountAttemptPreservesProviderDiagnostics|TestAsyncImageTaskCenterAccountAuditIsAdminOnlyAndRedacted' -count=1` 均通过。 |
| 完整验证 | `go test ./internal/handler -count=1` 通过；`go test ./internal/service -skip 'TestEstimateOpenAIInputTokens_CompareWithOpenAIAPI' -count=1` 通过（106.252s）；`pnpm typecheck`、`git diff --check` 和三份记录的 Markdown 本地链接检查通过。未排除时完整 Service 包仅 `TestEstimateOpenAIInputTokens_CompareWithOpenAIAPI` 的 `simple_user_text`、`system_plus_tool`、`multi_turn_text` 三个外部 API 对照子用例失败（各约 21s），与本次路径无关，未记为全量通过。 |
| 生产边界与未完成项 | 未连接或修改生产、未部署/重启、未重放真实 Gemini 请求、未回填历史任务；历史已被固定为 `Invalid request` 的任务无法恢复原文。账号级参考图上限与路由过滤仍待实现。 |

## 2026-09-20 生产 Gemini 异步 400 与通用错误文案诊断

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | `main`；完整 HEAD `defccfacba610450ac5f9fd06e0f54c2f25fbb7a`；`git describe --tags --always --dirty`=`v0.1.173.49-1-gdefccfa-dirty`；`backend/cmd/server/VERSION`=`0.1.173.49`。工作树开始时已有三份记录修改和四份未跟踪迁移文档，本轮保留并只增补三份记录，未提交/tag。 |
| 授权与生产范围 | 用户明确要求登录 `40.160.139.185` 检查 `sub2api` 异步生图日志。仅 SSH 读取 systemd 状态、`/opt/sub2api/logs`、运行配置的非敏感键和本机 PostgreSQL 任务/事件/运维表；未输出账号凭据或参考图 URL，未修改服务器文件、配置、数据或服务，未部署、重启、重放任务。会话于 `2026-09-20 19:43:39`（Asia/Shanghai）退出，服务仍为 `active`。 |
| 运行版本 | `/opt/sub2api/sub2api --version`：`0.1.173.49`，commit `52b0cf537f2e091506d52d72692195f6720088d6`，built `2026-09-15T09:59:36Z`；systemd 进程自 `2026-09-15 23:47:00 UTC` 运行。该 commit 对应本地 tag `v0.1.173.49`。 |
| 日志与任务证据 | 最近日志中的 Gemini `gemini-3-pro-image-preview` 均由 `gateway.cc.forward_failed` 写出 `Invalid request`，随后 `async_image.upstream_failed` 写出 `HTTP 400`。24 小时查询首个快照为 `succeeded=4739`、`failed=485`、`execution_unknown=4`、`invoking=2`，485 个失败按 error/message 分组完全相同；查询继续进行时失败增至 486，说明快照期间业务仍在运行。样本任务尝试记录有 selected/failed 账号与通用文案，但 `upstream_request_id` 为空。 |
| 参考图关联 | 24 小时滚动快照按远程参考图数聚合：`<=8` 为成功 `4675`、失败 `150`（约 `3.1%`）；`>8` 为成功 `64`、失败 `336`（`84.0%`）。大于 8 张的终态按账号：1 失败 125、25 失败 186、33 成功 64/失败 25。生产管理设置为 `max_reference_images=16`，源码模型能力表对 `gemini-3-pro-image-preview` 返回 14；9–14 张因而能进入队列，但上游账号实际能力显著不一致。该统计是强相关证据，不等于每一个 `<=8` 的 400 都是同一原因。 |
| 错误丢失链路 | 部署代码 `gemini_chat_completions_compat_service.go` 会提取上游 message；但 `mapGeminiErrorBodyToClaudeError` 明确保持通用消息、没有复制 `parsed.Error.Message`，400 分支随后把空映射消息固定为 `Invalid request`。`durable_async_image_worker.go` 对未命中引用图/容量/瞬时错误规则的非 2xx 使用最终兜底 `upstream_failed`。因此内部码只是失败阶段分类，不是上游原始错误码；公共任务查询虽可把通用 Invalid request 映射成应用码 604，但管理员任务记录仍保留内部字符串。 |
| 可追溯性结论 | `/opt/sub2api/config.yaml` 没有启用 `gateway.log_upstream_error_body`，当前/滚动日志没有 Gemini 原始 400 body；内部 Worker 的 httptest Gin 调用没有写入 `ops_error_logs`；486 个失败任务的 `request_payload` 均已按终态清理。故现存证据无法恢复这些历史 Gemini 400 的逐条精确上游原文，也不能断言上游原本没有详情。 |
| 本地文档验证 | `git diff --check` 通过；PowerShell 正则提取三份记录的 Markdown 本地链接并逐一 `Test-Path`，无断链。未运行代码测试或 CI。 |
| 建议与未完成项 | 该诊断阶段尚未实施代码；同日后续已在本地完成异步 Gemini message/code/request ID 持久化与 611–613 分类闭环（见上节），但未部署。账号能力参考图上限、候选过滤或全局限 8 仍未实施；未为排查开启可能含提示词/敏感字段的全量错误 body 日志。 |

## 2026-09-16 宝塔 PostgreSQL 公网连接只读诊断

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | `main`；完整 HEAD `defccfacba610450ac5f9fd06e0f54c2f25fbb7a`；`git status --short --branch` 为 `## main...origin/main` 加三份记录修改与四份既有迁移文档未跟踪；`git describe --tags --always --dirty`=`v0.1.173.49-1-gdefccfa-dirty`；`backend/cmd/server/VERSION`=`0.1.173.49`。保留本轮开始前全部未提交内容，未提交/tag。 |
| 授权与范围 | 用户明确要求登录 `40.160.139.185` 排查本机无法远程连接 PostgreSQL；仅 SSH 只读诊断、数据库系统目录/配置视图查询和本机 TCP 探测，未修改服务器文件、业务数据、防火墙或账号权限，未部署、重启生产。仅本地增补 readmenew.md、开发台账.md、agent.md；不记录登录口令。 |
| 验证命令/结果 | 本机 Python `socket.create_connection`：22 连通，5432/25432 均 `ConnectionRefusedError/WinError 10061`。远端 `ss -lntp`：PostgreSQL 仅 `127.0.0.1:25432`；`ufw status verbose`/`iptables -S`：TCP 5432/25432 已放行。`runuser -u postgres -- /www/server/pgsql/bin/psql -X -p 25432 -d postgres` 查询 `pg_settings`/`pg_file_settings`/`pg_hba_file_rules`：运行监听 `localhost`、`pending_restart=true`，文件监听 `*` 未应用，端口 25432 已应用，HBA 远程密码认证规则无解析错误。PostgreSQL 18.0，自 2026-09-07 06:14:02 UTC 运行；配置修改时间为 2026-09-16 12:56:03 UTC。`systemctl is-active sub2api` 为 active，连接统计见 6 个 sub2api idle 连接；此瞬时统计不代表全站业务无请求。 |
| 原因与处理 | 网络请求尚未进入数据库账号认证：监听配置改好但未重启生效，5432 也不是该实例实际端口。安装路径 `/www/server/pgsql`，数据目录 `/www/server/pgsql/data`，启动管理脚本 `/etc/init.d/pgsql`；重启方案为用户授权后 `runuser -u postgres -- /www/server/pgsql/bin/pg_ctl -D /www/server/pgsql/data -m fast -w restart`，短暂断开数据库连接，保持端口 25432。该命令尚未执行。 |
| 文档验证 | `git diff --check` 通过；本地 Python `Path.exists` 检查三份记录本轮新增的 3 个 Markdown 本地链接，全部存在。Git SHA/status/describe/VERSION 完成前复核与上述快照一致。远端日志 2026-09-16 12:56:03 和 12:59:26 UTC 均明确记录 `listen_addresses` 无法在不重启时修改；[PostgreSQL 18 官方配置说明](https://www.postgresql.org/docs/18/runtime-config-connection.html#GUC-LISTEN-ADDRESSES) 也确认该参数仅启动时生效。 |
| 未完成项 | PostgreSQL 重启、公网重新探测、真实数据库账号密码登录和应用重连验收待用户明确要求后执行。未运行代码测试、数据库迁移、CI、生产部署或应用重启。 |

## 2026-09-16 new-api 图片功能提取与迁移开发文档

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | `main`；完整 HEAD `defccfacba610450ac5f9fd06e0f54c2f25fbb7a`；`git status --short --branch` 为 `## main...origin/main` 加三份记录修改/四份新增文档；`git describe=v0.1.173.49-1-gdefccfa-dirty`；VERSION=`0.1.173.49`。提取前工作树干净、describe无dirty；当前仅本轮文档，未提交。 |
| 变更范围 | 根目录新增四份迁移开发包：[Codex任务书](newapi图片功能Codex开发任务书.md)、[总纲](newapi图片功能迁移开发总纲.md)、[公共API规范](newapi异步生图API兼容规范.md)、[页面业务规格](newapi图片页面与图库广场功能规格.md)。按源码提取全部新异步路径/响应/错误、用户与管理员任务中心、能力驱动工作台、本机实时图库、延期投稿/广场审核/举报、统一对象/清理及调度/计费。同步本文件、readmenew.md、agent.md；不修改功能代码、配置、测试、VERSION、发行身份或目标项目。 |
| 目标只读核对 | `F:/Code/Git/new-api`，完整 HEAD `69a50029819a26c53e6babd276d49cfe2f8880ad`，`## main...origin/main`，干净；Go/Gin/GORM与React/Rsbuild/Bun结构、Token/渠道/字符串分组、任务插件、内存计费会话、禁用产物存储入口已核对。结果仅用于迁移方案，不代表目标已开发/测试。 |
| 验证命令/结果 | PowerShell `[regex]::Matches` + `ConvertFrom-Json` 检查 4 份文档 16 个 JSON fenced 示例，全部通过；`Test-Path` 检查 65 个来源代码/迁移/专题路径，全部存在；开发包12个配套链接与三份记录12个新增链接通过，开发包无可点击外部文件依赖；代码围栏/尾随空格/末尾换行检查通过。`git diff --check` 通过；目标 `git rev-parse HEAD` / `git status --short --branch` 复核SHA不变、仍干净。 |
| 未完成项与运行边界 | 本任务只交付开发文档，new-api 实际开发/真实三数据库迁移/持久结算改造留给目标项目执行；未运行代码测试、浏览器、真实 Redis/数据库/OSS/上游、CI，未连接/修改/部署/重启生产。不存在本次生产授权或操作，未创建 Git 提交/tag。 |

## 2026-09-15 异步任务参考图 URL 持久化与管理员详情

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | 分支 `main`；完整 HEAD `d30dd111f2209df9c6e1fe681d86fdfe807cfd0e`；`git describe`=`v0.1.173.48-1-gd30dd11-dirty`；`backend/cmd/server/VERSION`=`0.1.173.48`；工作树包含上一项错误码 611–613 和本项参考图 URL 功能的代码、迁移、测试与文档，均未提交。 |
| 变更范围 | 新增 `226_ZJ_async_image_reference_urls.sql`，在任务行保存远程 HTTP(S) 参考图 URL JSON 数组；提交服务统一过滤非远程 URL，保留请求顺序与重复项。Repository 完成写入/扫描，管理员单任务详情返回 `reference_image_urls`，管理员页面在提示词下展示完整可点击链接并以新窗口打开；普通用户详情与所有列表不返回。同步中英文文案、接口文档、迁移 Wiki 和安全边界说明。 |
| 安全与兼容边界 | 内联 `data:` 图片不重复入库；历史任务默认空数组且不回填；URL 可能含临时签名参数，按敏感运维数据随任务保留期管理。新字段只在管理员单任务详情输出，前端再次限制为 HTTP(S)，外链使用 `noopener noreferrer`。 |
| 验证命令/结果 | `go test ./internal/service ./internal/repository ./internal/handler -count=1` 通过（109.605s / 2.546s / 37.951s）；Repository URL 读回/迁移/创建定向测试通过；前端 API/页面 Vitest 2 文件 13/13、`pnpm typecheck`、目标 ESLint、`pnpm build` 通过。构建仅有既有 pnpm overrides、Browserslist、动态导入和大 chunk 警告；完成前执行 `git diff --check` 与 Markdown 链接检查。 |
| 未完成项 | 未在隔离或生产 PostgreSQL 真实执行迁移 226；未验证已保存 URL 的目标仍有效；未连接真实 Redis、OSS 或上游做端到端任务；未做登录后浏览器视觉验收；未部署、未重启生产，Fork CI 未运行。 |

## 2026-09-15 异步生图独立错误码 611–613

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | 分支 `main`；完整 HEAD `d30dd111f2209df9c6e1fe681d86fdfe807cfd0e`；`git describe`=`v0.1.173.48-1-gd30dd11-dirty`；`backend/cmd/server/VERSION`=`0.1.173.48`；工作树只有本轮错误分类、测试和文档改动。 |
| 变更范围 | 异步失败查询新增 `611`（参考图超过上游 8 张限制）、`612`（缺少、未检测到或无法使用所需参考图）、`613`（提示词或输入图片无法处理）。分类同时适用于 `upstream_failed` 和带具体文案的 `invalid_reference_image`；BB/SC 共用同一响应函数，`fail_reason` 仍保留脱敏上游原文。同步两份接口文档和站内中英文指南中的中文说明与简短建议。 |
| 验证命令/结果 | `go test ./internal/handler -run 'TestAsyncImageFailureBusinessCode\|TestWriteBBQueryFailedIncludes' -count=1` 通过；`go test ./internal/handler -count=1` 通过（39.304s）；前端 `pnpm typecheck` 通过；完成前执行 `git diff --check` 和新增文档链接检查。 |
| 未完成项 | 未连接真实上游、Redis、PostgreSQL 或 OSS 做端到端任务查询；未部署或重启生产，Fork CI 未运行。生产生效需另行授权部署新二进制。 |

## 2026-09-13 生图账号池多维调度冒烟复核

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | 分支 `main`；完整 HEAD `38372af3ebf76fea96493f60cb49c35cbf5edd0a`；`git describe`=`v0.1.173.47-1-g38372af-dirty`；`backend/cmd/server/VERSION`=`0.1.173.47`。工作树包含生图账号池实现、本轮修复/测试/文档及本任务范围外的 `发行版发布与安装操作手册.md` 改动，后者未覆盖。 |
| 冒烟发现与修复 | 统一 Gemini 非方图的路由与计费短边档位；保存和运行时双层阻止独立池绕过 OAuth-only；强制 Antigravity 原生入口不再跳过图片意图，并按当前图片池判断单账号退避；Gemini 兼容入口能识别渠道别名映射后的生图模型但仍用请求侧 ID 作池键；Antigravity 分组拒绝无法被其强制调度器选择的 Gemini 账号；候选列表剔除通配 ID；迁移 225 新约束改为可重复执行。 |
| 计费边界 | 新账号池只约束候选集，不改 `CalculateImageCost`、图片档位价格、分组/用户/高峰倍率、独立图片倍率或异步结算状态机。Repository 的账号转换继续保留 `RateMultiplier`；测试断言 OAuth 过滤前后倍率不丢失、配置池过滤为空不回退，以及 Gemini `1792x1024` 路由与账单分类均为 1K。 |
| 验证命令/结果 | 六包完整测试 `go test ./internal/service ./internal/handler ./internal/handler/admin ./internal/repository ./internal/server/routes ./migrations -count=1` 通过（Service 103.662s、Handler 36.092s，其余通过）；多轮生图/计费定向测试通过；`go test -tags=unit ./internal/handler -run 'GeminiV1BetaModels\|ChatCompletions\|Image\|Billing' -count=1` 通过；`go test ./... -run '^$' -count=1` 与 `go test ./ent/... -run '^$' -count=1` 通过。前端定向 Vitest 5/5、`pnpm typecheck`、目标 ESLint、`pnpm build` 通过；构建只有既有 Browserslist、动态导入和大 chunk 警告。完成前执行 `git diff --check` 与 Markdown 链接检查。 |
| 生成说明 | 工作树内 `go generate ./ent` 两次因 Windows `user-mapped section open` 随机文件锁失败；每次中间产物均按实际差异恢复。随后在临时干净克隆中 `go generate ./ent` 成功，只同步 13 个由本次 schema 产生的预期文件；临时目录已删除，Ent 与全仓编译通过。 |
| 未完成项 | 本机未安装 `psql`/Docker，仓库配置指向非隔离外部服务，故未真实执行 222→225 迁移及 integration build-tag 数据库用例，也未启动服务做登录后亮/暗色、键盘和窄屏验收；未连接真实 Redis/上游/OSS，未部署或重启生产，Fork CI 未运行。 |

## 2026-09-13 生图账号池多维调度

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | 分支 `main`；完整 HEAD `38372af3ebf76fea96493f60cb49c35cbf5edd0a`；`git describe`=`v0.1.173.47-1-g38372af-dirty`；`backend/cmd/server/VERSION`=`0.1.173.47`；工作树包含本轮代码、生成文件、迁移、测试和文档，另有本任务范围外的 `发行版发布与安装操作手册.md` 改动，未覆盖。 |
| 变更范围 | 新增迁移 225 和 Ent 字段；`groups.image_account_pool_mode` 默认 `resolution`，原池表新增精确 `model` 维度及四列唯一键。新增管理员 `GET/PUT /groups/:id/image-account-pools`，PUT 事务内更新模式并全量替换三套绑定；旧接口只替换 `model=''` 清晰度行。分组复制同步模式和全部池行。统一路由上下文保留客户端模型与清晰度，接入 OpenAI Images、Gemini 原生/兼容、持久异步 Worker 及其 Composite 路径。管理端新增可键盘操作的三段模式、候选/手工模型卡片、三套配置无损切换、加载失败防覆盖及中英文文案；新增专题 Wiki。 |
| 验证命令/结果 | `go generate ./ent` 在隔离干净工作树成功生成并同步本仓库；`go test ./migrations ./internal/service ./internal/handler/admin -run 'ImageAccountPool\|ImageSizeAccount\|DuplicateGroupCopiesConfiguration' -count=1` 通过；`go test ./internal/service -run 'ImageSize\|ImageAccountPool\|DuplicateGroupCopiesConfiguration' -count=1` 通过；Handler/Repository/Service/Migrations 生图定向测试通过；`go test ./... -run '^$' -count=1` 全仓编译通过。前端 `pnpm test:run -- src/views/admin/__tests__/groupsImageAccountPools.spec.ts` 5/5；`pnpm typecheck`、目标 `pnpm exec eslint ...`、`pnpm build` 通过。构建仅有既有 Browserslist、动态导入和大 chunk 警告。完成前再次执行 `git diff --check` 和文档链接检查。 |
| 未完成项 | 未在隔离 PostgreSQL 真实执行 222→225 升级，integration build-tag 中的旧接口保留模型配置/分组复制用例未运行；未连接真实 Redis、上游或 OSS；本机配置不是隔离本地数据库/Redis，因此未启动服务做登录后亮色/暗色/键盘/窄屏视觉验收；未部署、未重启生产，Fork CI 未运行。 |

## 2026-09-09 OpenAI GPT Image 2.5 与图片工作台模型目录

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | 分支 `main`；HEAD `abb8a33d71aa5d05a5fc9437fee4d365582d43c5`；工作树包含本轮 OpenAI 2.5/工作台代码、测试、文档及此前未提交的发布手册改动；`git describe`=`v0.1.173.46-dirty`；VERSION=`0.1.173.44` |
| 变更范围 | `backend/internal/pkg/openai/constants.go` 默认目录新增 `gpt-image-2.5-flare`/`gpt-image-2.5-sunburst`；图片工作台返回模型级质量能力，2.5 暴露 `xhigh`/`max`；OpenAI 图片请求继续按 `gpt-image-*` 家族放行；管理端分组模型目录支持手动输入模型 ID、去重、勾选和排序；中英文文案同步。 |
| 验证命令/结果 | `go test ./internal/pkg/openai ./internal/service ./internal/handler -run 'Test(DefaultModelsIncludeGPTImage25Aliases|ImageWorkbenchCapabilitiesOpenAI|OpenAIGatewayServiceParseOpenAIImagesRequest_AllowsGPTImage25Quality|GatewayModels_CustomModelsList)' -count=1` 通过；`go test ./internal/handler -run 'TestGatewayModels_OpenAIGroupFallbackIncludesGPTImage25Models' -count=1` 通过；前端 `npm run test:run -- src/views/admin/__tests__/groupsModelsList.spec.ts src/features/image-workflow/__tests__/policy.spec.ts src/features/image-workflow/__tests__/ImageWorkbenchView.capabilities.spec.ts` 为 3 文件/27 项通过；`npm run typecheck`、`npm run build`、`git diff --check` 通过。构建仅有既有 Browserslist、动态导入和大 chunk 警告。 |
| 未完成项 | 未验证真实 OpenAI 账号权限、账号白名单与上游 2.5 生图；未连接真实 PostgreSQL/Redis/OSS 做端到端验收；未做登录后浏览器视觉验收；未部署或重启生产；Fork CI 未运行。 |

## 2026-09-09 管理员批量结束当前页异步生图任务

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | 分支 `main`；HEAD `b373fa5906abee97b90d0d4890264d1917c2371c`；工作树包含本轮代码、测试、文档及此前尚未提交的生图并发设置改动；`git describe`=`v0.1.173.44-1-gb373fa5-dirty`；VERSION=`0.1.173.44` |
| 变更范围 | 管理端 `/admin/async-image-tasks` 增加当前页批量结束按钮和确认框，只对当前页可结束任务创建稳定 ID 快照；新增 `POST /api/v1/admin/async-image-tasks/batch-terminate`，最多接收 100 个 ID，去重后逐条复用单任务 CAS、请求载荷清除、`admin_terminated` 错误码和 `admin_task_terminated` 事件；响应区分 `terminated`、状态终结/CAS 竞态导致的 `skipped` 与其他 `failed`，完成后刷新列表和统计；普通用户页面无此入口。 |
| 验证命令/结果 | `go test ./internal/service ./internal/handler ./internal/server/routes` 通过（Service 107.310s、Handler 36.987s、Routes cached）；`pnpm test:run -- src/features/async-image-tasks/__tests__/api.spec.ts src/features/async-image-tasks/__tests__/AsyncImageTasksView.spec.ts` 为 2 文件/11 项通过；目标 `pnpm exec eslint ... --fix`、`pnpm typecheck`、`pnpm build`、`git diff --check` 通过。构建仅保留既有 pnpm overrides、Browserslist、动态导入和大 chunk 警告。 |
| 未完成项 | 未做登录后浏览器视觉验收；未连接真实 PostgreSQL/Redis/上游/OSS 验证批量运行时与 Worker 竞态；未部署或重启生产；Fork CI 未运行。批量接口按项处理，调用方应以响应计数而非 HTTP 200 推断每项都已结束。 |

## 2026-09-09 生图并发系统设置与 Worker 上限调整

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | 分支 `main`；HEAD `b373fa5906abee97b90d0d4890264d1917c2371c`；工作树包含本轮代码、测试、Wire 生成和文档改动；`git describe`=`v0.1.173.44-1-gb373fa5-dirty`；VERSION=`0.1.173.44` |
| 变更范围 | 新增 `gateway_image_concurrency_settings` 单 JSON 设置及管理端 `GET/PUT/DELETE /admin/settings/image-concurrency`；系统设置优先、缺失/删除时回退启动时 `config.yaml`；OpenAI/Gemini 共享 limiter 每个新请求读取原子快照，Redis Pub/Sub 同步其他实例。系统设置页新增来源、有效值、YAML 回退值、保存和恢复入口。删除异步 Worker 前端 `max=64` 与后端 64 截断，保留 `<=0` 回到默认值和“修改后重启”语义。补齐 Wire 中图片账号熔断的正式 provider 接线，避免重新生成时丢失既有注入。 |
| 验证命令/结果 | `go generate ./cmd/server` 通过；`go test ./internal/handler ./internal/service ./internal/config ./internal/repository ./internal/handler/admin ./internal/server/routes ./cmd/server -count=1` 各包通过，其中 Handler 回归覆盖上限从 1 热改为 2 后 OpenAI/Gemini 新请求共享槽位；`pnpm typecheck`、`pnpm build` 通过；`pnpm test:run -- src/views/admin/settings/__tests__/ImageConcurrencySettingsCard.spec.ts src/views/admin/__tests__/SettingsView.spec.ts src/views/admin/__tests__/BackupView.spec.ts` 为 3 文件/46 项通过；目标 ESLint 0 error（保留 `SettingsView.spec.ts` 既有 4 条未使用 import warning）；Vite 开发服务器 `http://127.0.0.1:5173/` 根页面返回 HTTP 200；`git diff --check` 通过。 |
| 未完成项 | 未做真实浏览器视觉验收；未连接真实 PostgreSQL/Redis/上游/OSS 验证持久化、多实例通知或实际限流；未部署或重启生产；Fork CI 未运行。已有活动/等待请求不会因策略热更新被中断，Worker 数仍需重启才改变。 |

## 2026-09-08 异步生图冒烟复核与重试状态修复

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | 分支 `main`；HEAD `eecbd846cf2d0c5b0a1a25e07a226994e3711cab`；工作树包含本轮代码、测试和文档改动；`git describe`=`v0.1.173.43-1-geecbd84-dirty`；VERSION=`0.1.173.43` |
| 变更范围 | 新增 `AsyncImageTaskTransition.ClearAccountID`，重试回到 `queued` 和重新抢占 `invoking` 时清除旧当前账号，保留账号尝试审计；任务中心 Repository 筛选将未分配账号的内部 `invoking` 归入 `queued`，已分配账号才匹配 `invoking`；补充 Worker/Repository 回归测试。 |
| 验证命令/结果 | `go test ./internal/handler -count=1`、`go test ./internal/repository -count=1`、Service 异步定向测试与编译检查、前端异步 API Vitest `8/8`、`pnpm typecheck`、`git diff --check` 均通过。 |
| 未完成项 | 未做真实 PostgreSQL/Redis/上游/OSS 端到端验证，未部署或重启生产；状态筛选 SQL 未在生产数据上验收。 |

## 2026-09-08 异步生图未分配账号时状态展示修复

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | 分支 `main`；HEAD `eecbd846cf2d0c5b0a1a25e07a226994e3711cab`；工作树包含本轮 4 个 Go 文件、1 个前端视图和文档记录改动；`git describe`=`v0.1.173.43-1-geecbd84-dirty`；VERSION=`0.1.173.43` |
| 变更范围 | `asyncImagePublicStatus` 和任务中心视图新增展示归一化：内部 `invoking` 但 `account_id` 未分配时对外显示 `queued`；账号已分配时保持 `invoking`。同步架构/API 文档状态语义，新增公共查询、任务中心和账号已分配对照回归测试。 |
| 验证命令/结果 | `go test ./internal/handler -run 'TestAsyncImage|TestDurableAsyncImage' -count=1` 通过；`go test ./internal/service -run 'AsyncImage' -count=1` 通过；`pnpm test:run src/features/async-image-tasks/__tests__/api.spec.ts` 8/8 通过；`pnpm typecheck` 通过；`git diff --check` 通过。 |
| 未完成项 | 未做真实 PostgreSQL/Redis/上游端到端验证，未部署或重启生产；内部数据库状态仍保留 `invoking` CAS 语义。 |

## 2026-09-07 异步生图结果文件布局调整

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | 分支 `main`；HEAD `cc55ab9a181490ada4124d0eb84688a425d1ce43`；工作树含本轮异步结果 key 改动及既有 `sub2所需.md` 未提交改动；`git describe`=`v0.1.173.41-1-gcc55ab9-dirty`；VERSION=`0.1.173.41` |
| 变更范围 | `backend/internal/service/image_storage.go` 新增稳定异步结果对象 key 生成：结果直接位于 `results/YYYY/MM/DD/`，文件名为 `yyyyMMddHHmmss+GUID.ext`；`backend/internal/handler/durable_async_image_worker.go` 改用该规则；新增同任务多图不同名及重试稳定性的单测；同步 README、配置示例、架构和交接文档 |
| 验证命令/结果 | 定向测试及冒烟均通过：`go test ./internal/service -run 'ImageStorage|ImageObject|ImageResultUploader' -count=1`、`go test ./internal/handler -run 'AsyncImage|Upload' -count=1`、`go test ./internal/service ./internal/handler -run '^$' -count=1`；`git diff --check` 已执行，唯一提示来自既有 `sub2所需.md` 第 12 行尾随空格 |
| 未完成项 | 未做真实 OSS/数据库端到端验证，未部署或重启生产；旧对象不会自动迁移，仍按原对象 key 保留/清理 |

## 2026-09-05 个人图库同步广场上传修复

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | 分支 `main`；HEAD `0ea8f5195ff64e6acecb3d131d68eac8553f203b`；工作树含本轮及既有未提交改动；`git describe`=`v0.1.173.40-dirty`；VERSION=`0.1.173.38` |
| 变更范围 | `frontend/src/api/imageLibrary.ts` 删除同步上传和图库文件导入的手动 `multipart/form-data` 请求头，交由浏览器/Axios 自动生成 boundary；新增 `frontend/src/api/__tests__/imageLibrary.spec.ts` 回归测试 |
| 验证命令/结果 | `pnpm vitest run src/api/__tests__/imageLibrary.spec.ts src/features/image-workflow/__tests__/ImageLibraryPanel.spec.ts`（5/5）；`pnpm typecheck`；`pnpm eslint src/api/imageLibrary.ts src/api/__tests__/imageLibrary.spec.ts`；`pnpm build`；`go test ./internal/handler -count=1`（通过，37.030s）；`go test ./internal/service -run 'ImageLibrary|Submission' -count=1`；`git diff --check`；均通过 |
| 未完成项 | 未执行真实浏览器及 OSS/PostgreSQL/上游端到端验证；未部署或重启生产；构建仅有既有警告 |

## 2026-09-04 管理员异步任务搜索与列布局

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | 分支 `main`；HEAD `0ea8f5195ff64e6acecb3d131d68eac8553f203b`；工作树含本轮及既有未提交改动；`git describe`=`v0.1.173.40-dirty`；VERSION=`0.1.173.38` |
| 变更范围 | `buildAsyncImageTaskFilter` 增加最终账号名称和 ID 的 `ILIKE` 模糊匹配；管理员列表仅显示用户邮箱，用户列 `160px`、最终账号列 `200px`；同步中英文搜索提示和列标题；补充 Repository/API 回归测试 |
| 验证命令 | `pnpm vitest run src/features/async-image-tasks/__tests__/api.spec.ts`（8/8）；`pnpm typecheck`；目标 ESLint；`pnpm build`；`go test ./internal/repository -run 'TestBuildAsyncImageTaskFilterSearchIncludesFinalAccountName|TestAsyncImageTaskRepository' -count=1`；`go test ./internal/repository -count=1`；`go test ./internal/handler -run 'AsyncImageTask' -count=1`；`git diff --check` |
| 验证结果 | 全部通过；前端全量 Vitest `239` 文件/`1623` 用例通过，构建仅有既有 Browserslist、动态导入和大 chunk 警告；搜索参数仍只绑定一个模糊值 |
| 未完成项 | 未连接真实 PostgreSQL、未做真实上游/Redis/OSS 端到端验收，未部署或重启生产，Fork CI 未运行 |

## 2026-08-31 生产卡住任务清理（已授权）

- 服务器：`root@108.186.246.14`；范围：PostgreSQL `sub2api`，近 7 天创建且 `invoking` 执行/更新时间超过 1 小时。
- 执行：单事务 CAS 条件更新 7 个任务为 `failed`，`error_code=admin_terminated`，写入 `finished_at`、清空 `request_payload`，并追加 7 条 `admin_task_terminated` 事件。
- 复核：清理前 7 个符合条件；清理后 `stuck_nonterminal_over_1h=0`。当前运行中的 `invoking` 为新任务（分钟级），未重启或部署服务。
- 授权与边界：本次生产数据库状态变更由用户明确要求，仅处理超过 1 小时的近 7 天卡住任务；未改动 `execution_unknown` 等既有终态。

## 2026-08-31 异步生图卡住任务修复

- 基线：`git rev-parse HEAD=fd10de10fb3fe22e92e0ba7916ccd0433a8316c5`，`git status --short --branch=main...origin/main`（含本地未提交改动），`git describe=v0.1.173.36-1-gfd10de1-dirty`，`backend/cmd/server/VERSION=0.1.173.36`。
- 变更：修复超时 context 取消导致终态写入失败；恢复环按墙钟执行时限扫描 `invoking`；新增管理员终止服务、Handler 路由、前端按钮、API 测试和文档。
- 接口：`POST /api/v1/admin/async-image-tasks/{task_id}/terminate`，允许管理员将排队、调用中、执行未知及后处理失败任务 CAS 结束为 `failed`，记录 `admin_terminated`。
- 验证：`go test ./internal/service -run 'TestAsyncImageTask' -count=1`、`go test ./internal/handler -run 'Test.*AsyncImage' -count=1`、`go test ./internal/server/... -run '^$'`、前端 API Vitest 7/7、`pnpm run typecheck`、`git diff --check` 通过。完整 Service 包测试有既有外部 OpenAI token 对比用例 `TestEstimateOpenAIInputTokens_CompareWithOpenAIAPI` 失败，未归因于本次改动。
- 生产边界：仅完成本地代码与文档，未连接、修改、部署或重启 `108.186.246.14`，历史卡住任务未自动清理。

本台账记录 `JasonWangJie/sub2api_forimg` 的近期代码任务、文档同步和验证证据。它不是生产变更记录；生产服务器操作必须另行记录并明确授权。

## 当前基线

| 项目 | 值 |
|---|---|
| 记录日期 | `2026-09-04` |
| 分支 | `main` |
| 当前 HEAD | `0ea8f5195ff64e6acecb3d131d68eac8553f203b` |
| 当前 HEAD 说明 | `v0.1.173.40-dirty` |
| `git describe` | `v0.1.173.40-dirty` |
| `backend/cmd/server/VERSION` | `0.1.173.38` |
| 生产部署 | 已只读连接 `108.186.246.14` 查看服务和日志；本轮未修改、未部署、未重启 |

## 近期提交

| 日期 | SHA | 变更摘要 | 文档状态 |
|---|---|---|---|
| 2026-08-22 | `8d92b5d` | 异步参考图 OpenAI/Gemini 传输策略；透传、本地和混合回退；参考图、上游、容量分类重试；指数退避、抖动、`Retry-After`；任务阶段与计数迁移；管理端设置和前后端测试 | 已同步本台账、`readmenew.md`、`agent.md` |
| 2026-08-22 | `68602c4` | 图像大小账户池、异步协议尺寸路由和相关文档调整 | 已纳入总览 |
| 2026-08-20 | `aba0a7a` | 异步任务中心补充用户、API Key、分组和账号信息 | 已纳入总览 |
| 2026-08-19 | `a73469c` | 增加本地容量耗尽错误处理和测试 | 已纳入总览 |
| 2026-08-18 | `b622c33` | 仓储层与业务服务层错误处理重构 | 已纳入总览 |
| 2026-08-16 | `fdf99f2` | 新增 GroupImageSizeAccount 及迁移 `222_ZJ_group_image_size_accounts.sql` | 已纳入总览 |
| 2026-08-22 | 工作树文档更新 | 更新 `readmenew.md`，新增本台账、`agent.md`、`AGENTS.md`，并把同步要求写入 Cursor 规则 | `git diff --check` 通过；待随代码一并提交 |
| 2026-08-22 | 工作树任务中心统计 | `/admin/async-image-tasks` 与 `/async-image-tasks` 默认提交开始/结束日期为浏览器本地今天；列表接口按同一筛选条件聚合全局进行中、完成、失败和成功率；左上角新增绿色成功率 | 后端异步 Handler/Service/Repository 测试通过；前端 API/date 用例 7/7、typecheck 通过 |
| 2026-08-23 | 工作树异步生图统计接口 | 新增 `GET /v1/images/tasks_async/stats`，沿用 Authorization Bearer API Key 鉴权，按服务器时区统计当前用户当天全部异步任务，返回余额、请求数、成功数、失败数和成功率；新增 `异步生图接口文档new.md` 说明 | Handler、gateway route、middleware 定向测试通过；未部署生产 |
| 2026-08-24 | 异步生图生产线本地冒烟审查与 Gemini 透传修复 | 静态追踪 Outbox、Redis 租约、PostgreSQL 状态机、Worker 恢复、参考图传输、结果上传意图、计费、图库归档和清理链路；修复 Gemini `passthrough` 在构造请求处重复消费参考图预算的问题，并新增两张 HTTPS URL 在 `MaxReferenceImages=2` 下成功的回归测试 | `go test ./internal/service -run 'TestBuildGeminiAsyncChatBody|TestAsyncImage|TestOpenAIImages|TestImageStorage|TestPreparedUsage|TestGeminiAsync' -count=1`、`go test ./internal/handler ./internal/repository ./internal/server/routes ./internal/server/middleware -run 'TestAsyncImage|TestDurableAsyncImage|TestGatewayRoutes|TestIsAsyncImageTaskRead|TestRequestBodyLimit' -count=1` 均通过；此前前端异步相关 19/19、`pnpm typecheck` 已通过；真实上游/OSS/PostgreSQL/Redis/生产未验证 |
| 2026-08-24 | 异步生图模型级参考图预校验 | 复用批量生图能力表：Gemini Flash Image 已知模型上限 3、Pro Image 已知模型上限 14；异步提交阶段超过模型上限返回 `too_many_reference_images_for_model`，未知模型与 OpenAI 仍回退全局上限 | `go test ./internal/service -run 'TestBatchImage|TestAsyncImage|TestOpenAIImages' -count=1`、Handler/Repository/路由/中间件异步定向测试通过；真实上游模型能力未连接验证 |
| 2026-08-24 | `/guides/async-image-api` 与持久化异步 API 文档同步 | 页面补齐 Gemini BB `completions_gm`、Gemini SC `uploads/images_sc`、OpenAI `edits_oa`、统一任务查询与 `/tasks_sc` 兼容别名；补充 202/200 状态语义、幂等/重试、存储 URL 有效期、参考图 8/16 与 Flash 3/Pro 14 限制、上传配额和错误表；同步修正 `docs/DURABLE_ASYNC_IMAGE_API.md` 的 SC 旧响应体 | `pnpm typecheck`、`pnpm exec eslint src/views/user/GuideAsyncImageApiView.vue src/views/user/AsyncImageApiEndpointCard.vue src/views/user/guideAsyncImageApiContent.ts`、`pnpm build`、`git diff --check` 通过；构建仅有既有 chunk/Browserslist/动态导入警告；未做真实 API/OSS/生产验证 |
| 2026-08-25 | `b958648186fd9079d21111a7f32fa2a2a1a7566a` | 异步生图账号切换、账号尝试审计与超时对账状态 | 新增任务账号尝试历史/去重账号 ID/`reconciliation_status` 迁移；异步 Gemini 网络错误单账号快速换号；容量重试排除最近失败账号并在耗尽提示中报告尝试数；新增 Gemini 异步换号上限配置（默认 3）和 Worker 实际并发启动日志；前端高级设置增加换号上限 | 本轮定向冒烟：后端异步/Gemini/网关/迁移用例通过，五包及 `cmd/server` 编译检查通过，迁移包测试和 `go vet ./migrations` 通过；前端异步 Vitest 14/14、`pnpm typecheck`、目标 ESLint、`pnpm build` 通过；仅既有构建警告；未部署/重启生产，真实上游/对账接口仍待网关提供 |
| 2026-08-24 | `c296cd167800a3723b93f03f107032e4e55cc887` | 版本文件同步至 `0.1.173.33` | 仅更新 `backend/cmd/server/VERSION`；本轮通过读取实际版本文件和 Git 历史核对，未单独执行发布流程 |
| 2026-08-24 | `0336ab73973d1b36cecb4266f72f4072406f827d` | 持久异步 API 文档、Gemini BB/SC 与 OpenAI 编辑页面同步 | 文档和用户 API 页面覆盖 BB/SC 路由、查询状态、参考图限制、幂等/重试、错误和存储 URL 契约；本轮前端异步用例、类型检查、目标 ESLint 和生产构建通过；未做真实 API/OSS/生产验收 |
| 2026-08-25 | 工作树冒烟测试与交接同步 | 复验最近三次提交并更新本台账、`readmenew.md`、`agent.md` | `git diff --check` 通过；当前文档同步造成工作树 dirty；静态建议：任务中心尚未展示账号尝试/对账字段，最近失败账号应按唯一账号计数；两项均未在本轮修改 |
| 2026-08-25 | 管理员任务账号尝试审计展示与用户字段隔离 | `backend/internal/handler/async_image_task_center_handler.go` 增加管理员专用账号尝试摘要/历史、去重账号 ID、HTTP 状态、上游 request ID、失败原因和对账状态映射；用户视图不再返回完整上游 request ID；失败文本沿用 `logredact` 并补充 Authorization/api-key/token 头部脱敏；`AsyncImageTasksView.vue` 增加管理员摘要和折叠历史；同步中英文文案与类型；新增 Handler 边界测试 | `go test ./internal/handler -run 'AsyncImageTaskCenter' -count=1` 通过；前端异步 API/date 用例 7/7、`pnpm typecheck`、`pnpm build` 通过；`git diff --check` 通过；构建仅有既有警告；未部署/重启生产，真实上游对账未验证 |
| 2026-08-25 | 修正最近失败账号按唯一 ID 排除 | `backend/internal/handler/durable_async_image_worker.go` 的 `asyncImageRecentFailedAccountIDs` 改为按唯一失败账号 ID 计数，重复失败同一账号不再消耗 3 个账号名额；新增连续重复失败回归测试 | `go test ./internal/handler -run 'AsyncImage|Gateway' -count=1` 通过；`git diff --check` 通过；未部署/重启生产，真实账号轮换和上游对账未验证 |

## 2026-09-03 参考图拉取失败账号重试边界修复

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | 分支 `main`；HEAD `314fcc3c0055a3be0c652782b646e71ad75df808`；`git status --short --branch` 为 `main...origin/main` 加本地既有未提交改动；`git describe --tags --always --dirty`=`v0.1.173.38-1-g314fcc3-dirty`；VERSION=`0.1.173.38` |
| 变更范围 | Worker 合并任务历史与当前 invocation 的账号失败；同账号两次重试后只允许一次换号，第二账号失败即终止；OpenAI 图片调度优先使用预取 sticky 账号；新增 A/A/A/B 边界测试 |
| 验证 | `go test ./internal/handler -count=1` 通过；`go test ./internal/service -run 'TestMergeAsyncImageAccountAttempts|Test.*Image.*Circuit|Test.*ImageGenerationIntent' -count=1` 通过；`go test ./internal/repository -run 'AsyncImage|Migration' -count=1` 通过；Service/Repository `-run '^$'` 编译检查通过；`git diff --check` 通过 |
| 未完成项 | 完整 `go test ./internal/service -count=1` 仍因既有 `TestEstimateOpenAIInputTokens_CompareWithOpenAIAPI` 访问外部 OpenAI 超时失败；未做真实上游账号轮换、Redis/PostgreSQL/OSS、生产部署、重启或灰度验收 |

## 2026-09-04 参考图重试无缓存 sticky 修复

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | 分支 `main`；HEAD `314fcc3c0055a3be0c652782b646e71ad75df808`；`git status --short --branch` 为 `## main...origin/main` 加本地既有未提交改动；`git describe --tags --always --dirty`=`v0.1.173.38-1-g314fcc3-dirty`；VERSION=`0.1.173.38` |
| 变更范围 | OpenAI 调度器允许 Worker 预取 `StickyAccountID` 绕过 Redis sticky 缓存依赖；新增无缓存 sticky 回归测试，保证参考图拉取超时的同账号两次重试稳定执行 |
| 验证 | `go test ./internal/service -run 'TestOpenAIGatewayService_SelectAccountWithScheduler_(SessionSticky|PrefetchedStickyWithoutCache)$' -count=1` 通过；`go test ./internal/handler -run 'TestAsyncImageReferenceRetry|TestRecordAsyncImageReferenceFetchFailure|TestShouldFallbackHybridReferenceTransport' -count=1` 通过 |
| 完整 Service 结果 | `go test ./internal/service -count=1` 失败仅因既有 `TestEstimateOpenAIInputTokens_CompareWithOpenAIAPI` 请求 `api.openai.com` 连接超时；与本地无缓存 sticky 改动无关 |
| 未完成项 | 未执行真实上游、Redis/PostgreSQL/OSS、生产部署、重启或灰度验收 |

## 2026-09-04 清晰度账号池 priority 回显修复

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | 分支 `main`；HEAD `314fcc3c0055a3be0c652782b646e71ad75df808`；`git status --short --branch` 为 `## main...origin/main` 加本地既有未提交改动；`git describe --tags --always --dirty`=`v0.1.173.38-1-g314fcc3-dirty`；VERSION=`0.1.173.38` |
| 变更范围 | 修复前端清晰度账号池回显丢失 priority；现在 `1:1,32:1` 保存后仍保留优先级；确认后端允许多个账号共享 priority，并新增同优先级调度回归测试 |
| 验证命令 | `pnpm exec vitest run src/views/admin/__tests__/groupsImageAccountPools.spec.ts src/features/async-image-tasks/__tests__/api.spec.ts`；`pnpm typecheck`；`go test ./internal/service -run 'ImageSize|GroupImage|Image.*Pool|GatewayImageSize' -count=1`；`go test ./internal/handler/admin -run 'GroupHandlerImageSizeAccounts|ImageSize' -count=1`；`go test ./internal/repository -run 'ImageSize|Migration' -count=1`；`git diff --check` |
| 验证结果 | 前端 11/11、类型检查、后端图片账号池/管理员接口/迁移和同优先级调度测试通过 |
| 未完成项 | 未执行浏览器实机、真实数据库/上游/Redis/OSS、生产部署、重启或灰度验收 |

## 2026-09-04 深度冒烟复验

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | 分支 `main`；HEAD `314fcc3c0055a3be0c652782b646e71ad75df808`；`git status --short --branch` 为 `## main...origin/main` 加本地既有未提交改动；`git describe --tags --always --dirty`=`v0.1.173.38-1-g314fcc3-dirty`；`VERSION`=`0.1.173.38` |
| 复验范围 | 清晰度账号池 `1:1,32:1` 回显、相同优先级账号调度、异步任务相关 Handler/Repository/路由，以及前端构建链路 |
| 通过 | `go test ./internal/handler -count=1`；图片账号池/管理员接口/Repository 迁移定向测试；`go test ./internal/server/routes ./internal/server/middleware -count=1`；`go test ./... -run '^$' -count=1`；前端账号池+异步任务 API `11/11`；`pnpm typecheck`；目标 ESLint；`pnpm build`；`git diff --check` |
| 完整 Service 结果 | `go test ./internal/service -count=1` 失败于既有 `TestEstimateOpenAIInputTokens_CompareWithOpenAIAPI` 的 3 个子用例，访问 `api.openai.com/v1/responses/input_tokens` 连接超时；本次账号池定向 Service 测试通过 |
| 未完成项 | 未执行浏览器实机、真实上游/Redis/PostgreSQL/OSS、生产部署、重启或灰度验收；构建警告为既有提示 |

## 2026-08-29 异步生图错误审计与查询对照码

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | 分支 `main`；HEAD `fd10de10fb3fe22e92e0ba7916ccd0433a8316c5`；`git describe --tags --always --dirty`=`v0.1.173.36-1-gfd10de1-dirty`；`VERSION`=`0.1.173.36` |
| 代码变更 | `durable_async_image_handler.go` 失败查询新增整数 `error_code` 601-609；上游失败优先返回 Worker 已脱敏 `ErrorMessage`；补充内容政策、参考图、容量、限流、输入、超时、输出解析、存储/计费分类；新增 Handler 回归测试 |
| 文档变更 | 同步 `docs/DURABLE_ASYNC_IMAGE_API.md`、`异步生图接口文档new.md`、`frontend/src/views/user/guideAsyncImageApiContent.ts`、`readmenew.md`、`agent.md`；前端轮询类型同步 `frontend/src/api/imageWorkbench.ts` 与 `frontend/src/views/user/ImageWorkbenchView.vue` |
| 生产只读审计 | 连接 `root@108.186.246.14`，统计 `/opt/sub2api/logs/sub2api.log` 及滚动日志；`2026-08-22 00:00` 至 `2026-08-29 11:45` 共 2312 条 `async_image.upstream_failed`；未修改、未部署、未重启 |
| HTTP 分布 | 400:1526，502:319，503:125，504:114，429:113，524:50，403:64，408:1 |
| 主要分类 | 参考图 URL 拉取 1231；账号/容量耗尽 733；Gemini `Invalid request` 154；第三方相似性 36；内容安全/政策至少 29；输入问题 16；通用失败 4（关键词分类可能重叠） |
| 验证 | `go test ./internal/handler` 通过；`go test ./internal/handler -run 'AsyncImageFailure|WriteBBQuery' -count=1` 通过；`pnpm typecheck` 通过；`git diff --check` 通过 |
| 未完成项 | 本轮未执行生产部署/重启、真实上游或 OSS 端到端验证、前端生产构建；6xx 为应用层分类码，不是 HTTP 状态码 |

## 2026-08-31 异步生图接口文档更新

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | 分支 `main`；HEAD `fd10de10fb3fe22e92e0ba7916ccd0433a8316c5`；`git describe --tags --always --dirty`=`v0.1.173.36-1-gfd10de1-dirty`；`VERSION`=`0.1.173.36` |
| 文档变更 | 更新 `异步生图接口文档new.md`：补充 Base URL、Retry-After 轮询建议、失败字段契约、HTTP 408/429/502/503/504/524 与 601-609 的关系、上游原文样例、生产错误快照；删除重复的 503 状态行 |
| 验证 | `git diff --check` 通过；本轮未重新执行测试，沿用前一轮已通过的 `go test ./internal/handler`、定向 Handler 测试和 `pnpm typecheck` |
| 生产边界 | 仅同步本地文档，未连接、修改、部署或重启生产 |

## 2026-08-31 生产异步生图卡住任务只读诊断

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | 分支 `main`；HEAD `fd10de10fb3fe22e92e0ba7916ccd0433a8316c5`；`git describe --tags --always --dirty`=`v0.1.173.36-1-gfd10de1-dirty`；`VERSION`=`0.1.173.36` |
| 生产范围与授权 | 按用户明确提供的 root 凭据只读连接 `108.186.246.14`；查看 systemd、日志、PostgreSQL、Redis 和配置；未修改任务/配置/数据库，未部署、未重启 |
| 状态快照 | `2026-08-31T13:25:34Z`：`succeeded=13626`、`failed=1278`、`execution_unknown=60`、`invoking=8`；7 个 `invoking` 超过 120 秒租约，全部 Gemini `gemini-3-pro-image-preview` |
| 根因证据 | 7 个任务仅有 `queued -> invoking`；日志出现 `execution_timeout_cancel timeout=900000`，随后 `execution_unknown_transition_failed error=context canceled`。Worker 使用已取消 context 写终态，导致状态落库失败并长期停在 `invoking` |
| 近期失败分类 | 近 7 天：`upstream_failed=356`、`invalid_reference_image=297`、`local_capacity_exhausted=63`、`execution_timeout=46`、`upstream_capacity_exhausted=27`、`execution_unknown=34` |
| 运行参数与依赖 | 后台 `image_storage_config.async_image`：`worker_concurrency=50`、`worker_lease_seconds=120`、`recovery_interval_seconds=30`、`execution_timeout_seconds=900`；Redis PING 正常，PostgreSQL 无长 SQL 证据 |
| 文档变更 | `异步生图接口文档new.md`、`docs/DURABLE_ASYNC_IMAGE_API.md` 增加卡住任务诊断、状态快照、日志证据和未执行修复说明；同步 `readmenew.md`、`agent.md` |
| 验证 | 生产查询命令已执行并记录结果；本地 `git diff --check` 待本轮末执行；未重新执行代码测试或生产端到端验证 |
| 未完成项 | 尚未修复已取消 context 的终态更新、未人工收敛 7 个任务、未重启或部署；修复需另行授权并先评估对账/重复计费风险 |

版本号同步提交（`4c94fdd`、`68602c4`、`8c916e8`、`3a8a867` 等）只更新发行版本文件，具体以 Git 历史和 `VERSION` 文件为准。

## 本次异步生图功能

- OpenAI 参考图支持 `passthrough`、`local`、`passthrough_fallback_local`，默认混合模式。
- Gemini 默认 URL 透传，`local` 模式转换为 `inlineData`。
- data URI 始终本地校验；OpenAI edits 的本地方式使用 multipart。
- 参考图抓取、上游临时错误、账号容量错误使用独立重试计数，并受总重试上限约束。
- `execution_unknown` 表示上游请求结果不确定，不自动重放。
- 单张参考图默认及硬上限为 80,000,000 像素；任务总像素默认仍为 80,000,000。
- `/admin/settings` 保存的运行参数由任务处理路径重新读取；Worker 数量在进程启动时创建，修改后需重启。
- 新增 `async_image.auto_archive_to_library`，默认 `false`；关闭时成功结果仍保存到配置的本地/OSS/Superbed 对象存储，并由异步查询返回签名或公开 URL，不创建 `image_library_items`，不触发 `IMAGE_LIBRARY_BYTE_QUOTA`。开启后才写入 `library_archive` Outbox。
- 新增 `GET /v1/images/tasks_async/stats`：使用 `Authorization: Bearer <API_KEY>`，按服务器配置时区计算用户当天任务汇总；`today_requests` 包含处理中、成功和失败，`success_rate` 为百分比。

## 验证记录

最近一次本地冒烟验证：2026-08-25。

| 验证 | 结果 |
|---|---|
| `go test ./internal/handler` 异步定向用例 | 通过 |
| `go test ./internal/service` 异步配置/协议用例及 Gemini 透传预算回归 | 通过 |
| `go test ./internal/repository` 异步任务/迁移用例 | 通过 |
| `pnpm test:run -- src/views/admin/asyncImageRuntimeConfig.test.ts` | 3/3 通过 |
| `pnpm typecheck` | 通过 |
| `pnpm build` | 通过；仅有既有 chunk/Browserslist/动态导入警告 |
| `go test ./internal/service -run 'TestAsyncImageRuntimeConfig|TestAsyncImageTaskService' -count=1` | 通过 |
| `go test ./internal/repository -run 'TestAsyncImageTaskRepository(TransitionUsesVersionCASAndEvent|SuccessSkipsLibraryArchiveWhenDisabled|SuccessEnqueuesLibraryArchiveWhenEnabled)' -count=1` | 通过 |
| `go test ./internal/handler ./internal/repository ./internal/server/routes ./internal/server/middleware` 异步定向用例 | 通过 |
| `pnpm test:run -- src/views/admin/asyncImageRuntimeConfig.test.ts` | 3/3 通过（新增开关默认值断言） |
| `git diff --check` | 通过 |
| `pnpm exec eslint src/views/user/GuideAsyncImageApiView.vue src/views/user/guideAsyncImageApiContent.ts` | 通过 |
| `go test ./internal/handler -run 'TestAsyncImageTaskCenterGetStats|TestAsyncImageTaskCenterListForUser|TestAsyncImageTaskCenterGetForUser' -count=1` | 通过 |
| `go test ./internal/server/routes -run 'TestGatewayRoutesDurableAsyncImagePathsAreRegistered|TestGatewayRoutesAsyncImagesPathsAreRegistered' -count=1` | 通过 |
| `go test ./internal/server/middleware -run TestIsAsyncImageTaskRead -count=1` | 通过 |
| `go test ./internal/handler -run 'AsyncImageTaskCenter' -count=1` | 通过；管理员审计字段、用户隐藏字段和失败文本脱敏边界通过 |
| `pnpm test:run -- src/features/async-image-tasks/__tests__/api.spec.ts src/features/async-image-tasks/__tests__/dateFilters.spec.ts` | 7/7 通过 |
| `pnpm typecheck` | 通过 |
| `pnpm build` | 通过；仅有既有 pnpm overrides、Browserslist、动态导入和大 chunk 警告 |
| `git diff --check` | 通过 |
| `go test ./internal/handler -run 'AsyncImage|Gateway' -count=1` | 通过；包含唯一失败账号排除回归测试 |

未完成或不能宣称的项目：未连接生产环境；未执行真实上游账号、真实 OSS、真实 PostgreSQL/testcontainers 验证；Fork CI 结果需在 GitHub Actions 页面核对。

本次审查的下一步：为上游执行超时补充“已产生上游成本但本地未落账”的主动观测/对账接口；增加生成结果总数量和总字节保护，并在隔离环境完成 Redis、PostgreSQL、对象存储和可控上游的端到端演练。管理员任务审计页面和唯一失败账号排除修正已完成静态及边界测试，但真实上游对账、账号轮换和生产运行时验收仍未完成。

补充：一次完整 `go test ./internal/handler ./internal/service ./internal/repository` 中，Handler/Repository 通过；Service 仅失败于既有的外部 OpenAI token 对比用例，因外部网络/API 不可用，与本次统计改动无关。异步相关定向 Service 用例通过。

## 台账维护规则

每次任务完成必须同时更新 `readmenew.md`、本文件和 `agent.md`，至少写入实际日期、完整 SHA、工作树状态、变更摘要、验证命令/结果和未完成项。文档更新本身也要经过 `git diff --check`，不得把未验证内容写成“已通过”或“生产可用”。

## 2026-08-27 Gemini 异步 400 换号竞态修复

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | `main...origin/main`；HEAD `3c682ec2e346042229b6551d3346e3e8e363cff9`；`git describe`=`v0.1.173.35-1-g3c682ec-dirty`；VERSION=`0.1.173.35` |
| 变更范围 | Gemini 异步 400 账号级错误在响应映射前返回未提交的 `UpstreamFailoverError`；兼容纯文本/包装错误及 ErrorPolicy 路径；保留上游 request ID；完整响应的 MIME/容器/Base64/无图片错误改记 `upstream_invalid_output` |
| 错误边界 | 参考图抓取、像素/格式、内容政策、参数错误不触发账号级 400 换号；无完整响应仍为 `execution_unknown`，不自动重放 |
| 已验证 | `go test ./internal/service -run 'TestGeminiForwardAsChatCompletionsAsyncInvalidRequestDoesNotCommitResponse|TestGeminiForwardAsChatCompletionsAsyncImageFailsOverAfterOneAttempt|TestGeminiAsyncAccountFailover400|TestNewGeminiAsyncAccountFailover400' -count=1` 通过；`go test ./internal/handler -run 'TestAsyncImageInvalidOutputErrorIsNotExecutionUnknown|TestAsyncImageAccountAttemptTimeout|TestAsyncImageExplicitReferenceFetchFailure' -count=1` 通过；`go test ./internal/handler` 通过；`git diff --check` 通过 |
| 未完成/边界 | 本地改动尚未构建或部署到生产；未做真实上游三账号端到端轮换验证；Service 完整包测试若包含外部凭证用例，必须按最终命令输出记录；未重启生产 |
| 交接 | 生产当前运行的二进制仍是已部署版本，不包含本地未部署修复；后续需先复核完整 Service 测试，再执行带 `-tags embed` 构建和经授权的部署验收 |

## 2026-08-27 本轮补充验证与 request ID 修复

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | `main...origin/main`；HEAD `3c682ec2e346042229b6551d3346e3e8e363cff9`；`git describe`=`v0.1.173.35-1-g3c682ec-dirty`；VERSION=`0.1.173.35` |
| 变更范围 | 池模式 ErrorPolicy failover 返回完整响应头，保证异步任务记录上游 request ID；Gemini 异步 400 分类器仅对明确 `Invalid request` 文本换号，空响应保持普通错误语义 |
| 已验证 | `go test ./internal/service -run 'Gemini|ErrorPolicy' -count=1` 通过；`go test ./internal/handler -run 'AsyncImage|Gateway' -count=1` 通过；`go test ./internal/handler` 通过；`go test ./internal/repository ./internal/server/routes ./internal/server/middleware` 通过；`go build -tags embed ./cmd/server` 通过；`git diff --check` 通过 |
| 未完成/边界 | Service 全量测试若包含外部 OpenAI token/API 用例仍可能失败，不能写成全包通过；未部署、未重启生产，未做真实上游账号轮换与结果对账验收 |

## 2026-08-26 异步生图参考图与账号重试修正

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | `main...origin/main`；HEAD `22fc75bb1e8f231e57b953cfc7c0aad3e5c50a9a`；`git describe`=`v0.1.173.34-1-g22fc75b-dirty`；VERSION=`0.1.173.34` |
| 代码范围 | OpenAI/Gemini 异步 400 换号分类；参考图上游抓取失败识别与混合模式本地回退；Gemini messages 路径对齐；本地参考图下载闸门二次缓存检查；账号尝试/请求 ID/超时状态持久化及 300 秒单账号超时切换 |
| 参考图处置 | `image_url fetch failed`、`download OpenAI reference image`、带远程抓取上下文的 `INVALID_IMAGE` 容器错误和 TLS/网络抓取错误触发混合模式本地下载；OpenAI multipart、Gemini inlineData；裸 multipart 图片损坏不回退；回退阶段持久化，后续重试不切回 URL |
| 已执行验证 | `go test ./internal/handler -run 'AsyncImage|Failover|Gemini.*Async|OpenAI.*Image' -count=1` 通过；`go test ./internal/service -run 'AsyncImage|Gemini.*(Async|Image)|OpenAI.*(Image|Upstream)|Reference|Failover' -count=1` 通过；`go test ./internal/repository -run 'AsyncImage|Migration' -count=1` 通过；`pnpm vitest run` 239 文件/1620 断言通过；`pnpm typecheck` 通过；`pnpm build` 通过；`git diff --check` 通过 |
| 未完成/边界 | 未连接、修改或重启生产；未做真实上游网关账号轮换、结果对账、Redis/PostgreSQL、OSS 和 Fork CI 验收；构建仍有既有 Browserslist、动态导入和大 chunk 警告；`execution_unknown` 仍禁止自动重放 |
| 交接 | 三份记录已同步；`diff-review.txt` 为既有未跟踪文件，保留不删除 |

## 2026-08-31 近48小时异步生图错误分类补充

| 项目 | 记录 |
|---|---|
| 生产只读范围 | `root@108.186.246.14` PostgreSQL 与 `journalctl -u sub2api`，服务器 UTC 最近 48 小时；未部署、未重启 |
| 错误统计 | `upstream_failed=103`、`invalid_reference_image=70`、`upstream_capacity_exhausted=3`、`execution_unknown=2`、`admin_terminated=6` |
| 新发现 | HTTP 400 文案 `上游生图失败（HTTP 400）：由于我这边发生了错误，我未能生成图片。` 不属于明确输入错误，新增容错 `error_code=610`；未知内部错误同样归 610，`fail_reason` 保留脱敏原文。`invalid_reference_image` 中的像素/尺寸/MIME 错误改归 604，网络拉取错误仍为 602 |
| 代码与文档 | 更新 `durable_async_image_handler.go`、Handler 测试、异步接口文档与用户指南 |
| 验证 | `go test ./internal/handler -run 'TestAsyncImageFailure|TestWriteBBQueryFailedIncludesTaskIDAndFailReason' -count=1`、`go test ./internal/handler -count=1`、`git diff --check` 通过 |
| 未完成项 | 生产仍运行旧二进制；待用户另行授权后再构建、部署和重启 |

## 2026-08-31 异步接口冒烟、安全与缺陷审查

| 项目 | 记录 |
|---|---|
| 审查范围 | 本地异步 Handler/Service 状态机、超时终态、恢复扫描、管理员终止、路由、错误分类和前端类型检查 |
| 修复 | Chat Completions 异步兼容服务未配置的提前返回路径显式取消 account-attempt context，消除潜在资源泄漏 |
| 安全结论 | AdminAuth/合规中间件保护管理员接口；终止使用状态+版本 CAS；`execution_unknown` 不自动重放；`610` 仅作未知错误容错码 |
| 验证 | Handler/Service 异步定向测试、`go test ./internal/handler -count=1`、`go vet ./internal/handler ./internal/service`、`pnpm typecheck`、`git diff --check` 通过 |
| 剩余风险 | 真实上游/Redis/PostgreSQL/OSS 端到端、部署后告警和账单对账未验证；生产未部署本轮改动 |

补充分类：真实人物露骨亲密、性化、情色/色情、裸露等上游 HTTP 400 文案现归 `error_code=601`，并保留原始脱敏 `fail_reason`；回归测试通过。
本轮追加精确文案“违反了关于裸露、色情或情色内容的防护限制”回归用例，仍归 `601`。

补充生产只读复核：`2026-08-31 14:50 UTC` 当前 `invoking=3`，`coalesce(started_at,created_at) < now()-1h` 为 `0`；未发现新的长时间卡住任务。

## 2026-09-03 管理端异步任务中心列表紧凑化

| 项目 | 记录 |
|---|---|
| 实际基线 | 分支 `main`；HEAD `314fcc3c0055a3be0c652782b646e71ad75df808`；`git describe --tags --always --dirty`=`v0.1.173.38-1-g314fcc3-dirty`；`backend/cmd/server/VERSION`=`0.1.173.38` |
| 变更范围 | `frontend/src/features/async-image-tasks/AsyncImageTasksView.vue`：管理员列表收窄任务号、状态、图片/存储列，移除状态进度条，在实际费用前增加最终账号列 |
| 验证命令 | `cd frontend; pnpm typecheck`；`pnpm test:run src/features/async-image-tasks/__tests__/api.spec.ts`（7/7）；`pnpm exec eslint src/features/async-image-tasks/AsyncImageTasksView.vue`；`pnpm build`；`git diff --check` |
| 验证结果 | 上述命令均通过；构建仅有既有 Browserslist、动态导入和大 chunk 警告，未执行生产连接、部署或重启 |
| 当前工作树 | `## main...origin/main`；`agent.md`、`readmenew.md`、`开发台账.md` 和目标 Vue 文件为本轮修改 |
| 未完成项 | 浏览器实机视觉验收、前端生产构建及生产环境验证仍待授权后单独执行 |

## 2026-09-03 异步任务中心平均耗时统计

| 项目 | 记录 |
|---|---|
| 实际基线 | 分支 `main`；HEAD `314fcc3c0055a3be0c652782b646e71ad75df808`；`git describe --tags --always --dirty`=`v0.1.173.38-1-g314fcc3-dirty`；`backend/cmd/server/VERSION`=`0.1.173.38` |
| 变更范围 | `backend/internal/service/async_image_task.go`、`backend/internal/repository/async_image_task_repo.go`、`backend/internal/handler/async_image_task_center_handler.go` 增加 `average_duration_ms` 统计；`frontend/src/features/async-image-tasks/AsyncImageTasksView.vue`、API/types 与中英文案增加顶部平均耗时 |
| 统计口径 | 当前筛选条件下仅统计 `status='succeeded'` 且 `finished_at IS NOT NULL` 的任务，平均 `finished_at - submitted_at`；无样本返回 `null`，页面显示 `-` |
| 验证命令 | `go test ./internal/service -run 'TestAsyncImageTask' -count=1`；`go test ./internal/repository -run 'TestAsyncImageTask' -count=1`；`go test ./internal/handler -run 'TestAsyncImageTask' -count=1`；`cd frontend; pnpm test:run src/features/async-image-tasks/__tests__/api.spec.ts`；`pnpm typecheck`；目标文件 ESLint；`pnpm build`；`git diff --check` |
| 验证结果 | 定向后端三包通过；前端 API 8/8、类型检查、ESLint、生产构建和 diff 检查通过。整包 `go test ./internal/service ./internal/repository ./internal/handler -count=1` 的 Service 受既有外部 OpenAI token 对比用例失败，Repository/Handler 通过 |
| 当前工作树 | `## main...origin/main`；异步任务统计代码、测试及 `agent.md`、`readmenew.md`、`开发台账.md` 为修改项；未部署生产 |
| 未完成项 | 浏览器实机视觉验收、真实数据库/上游/OSS 链路、生产部署与重启仍未执行 |

## 2026-09-03 生图账号调度去黏性与清晰度池负载均衡

| 项目 | 记录 |
|---|---|
| 实际基线 | 分支 `main`；HEAD `314fcc3c0055a3be0c652782b646e71ad75df808`；`git describe --tags --always --dirty`=`v0.1.173.38-1-g314fcc3-dirty`；`backend/cmd/server/VERSION`=`0.1.173.38` |
| 变更范围 | 新增 Gemini 生图会话策略辅助函数；OpenAI 图片端点、Responses/Responses WebSocket、Chat Completions 生图清空会话键并跳过 `previous_response_id` 账号黏性；Gemini 无签名生图跳过摘要会话，带 `thoughtSignature` 保留黏性；更新清晰度账号池中英文提示与优先级示例 |
| 调度语义 | 普通文本请求保持黏性；生图按账号池优先级、有效负载因子实时负载和 LRU 调度；不同优先级严格回退，同优先级负载均衡 |
| 验证命令 | `go test ./internal/service -run 'TestGeminiImageStickySessionRequired|TestIsGeminiNativeImageGenerationIntent|TestIsImageGenerationIntent|TestGatewayImageSizePool|Test.*Scheduler' -count=1`；`go test ./internal/handler -run 'Test.*(Gemini|OpenAI.*Image|Responses|ChatCompletions|Gateway)' -count=1`；`go test ./internal/repository -run 'Test.*(Image|Async)' -count=1`；`cd frontend; pnpm typecheck`；`pnpm eslint src/i18n/locales/zh/admin/overview.ts src/i18n/locales/en/admin/overview.ts`；`pnpm build`；`git diff --check` |
| 验证结果 | 后端三组定向测试、前端类型检查、locale ESLint、构建和文档写入后的 `git diff --check` 均通过；构建仅有既有 Browserslist、动态导入和大 chunk 警告 |
| 未完成项 | 未执行完整 Service 包、浏览器实机、真实 Redis/PostgreSQL/上游账号轮换、生产部署或重启；生产仍运行旧二进制 |

## 2026-09-03 生图调度冒烟验证

| 项目 | 记录 |
|---|---|
| 实际基线 | 分支 `main`；HEAD `314fcc3c0055a3be0c652782b646e71ad75df808`；`git describe --tags --always --dirty`=`v0.1.173.38-1-g314fcc3-dirty`；VERSION=`0.1.173.38` |
| 冒烟范围 | Gemini 生图黏性例外与意图识别、清晰度账号池/调度器、OpenAI/Gemini Handler、路由注册、前端异步任务 API 和类型检查 |
| 验证命令 | `go test ./internal/service -run 'TestGeminiImageStickySessionRequired|TestIsGeminiNativeImageGenerationIntent|TestIsImageGenerationIntent|TestGatewayImageSizePool|Test.*Scheduler' -count=1`；`go test ./internal/handler -run 'Test.*(Gemini|OpenAI.*Image|Responses|ChatCompletions|Gateway)' -count=1`；`go test ./internal/server/routes -count=1`；`cd frontend; pnpm test:run src/features/async-image-tasks/__tests__/api.spec.ts`；`pnpm typecheck` |
| 验证结果 | Service、Handler、Routes 通过；前端 API 8/8、类型检查通过 |
| 下一步 | 隔离环境验证同优先级负载均衡、跨优先级回退、Gemini `thoughtSignature` 连续请求和 failover；补充调度指标及账单/request ID 对账后，再经授权灰度部署 |
| 未完成项 | 未执行真实上游/Redis/PostgreSQL/OSS、浏览器实机、生产部署、重启或灰度验收；生产仍运行旧二进制 |

## 2026-09-03 参考图拉取失败换号重试

| 项目 | 记录 |
|---|---|
| 实际基线 | 分支 `main`；当前 HEAD 以最终命令输出为准；版本文件 `backend/cmd/server/VERSION` 以最终命令输出为准 |
| 变更范围 | Durable async image worker 记录 `image_url fetch failed` 失败账号；任务级 sticky 保持同账号两次重试；第三次同账号失败后排除当前账号并只允许一次换号重试；OpenAI Images 与 Gemini Chat Completions 路由禁用历史排除回放 |
| 验证命令 | `gofmt -w` 相关 Go 文件；`go test ./internal/handler -count=1`；定向 `go test ./internal/service`；`git diff --check` |
| 验证结果 | Handler 全量通过；Gemini/OpenAI 参考图分类用例通过；新增账号重试策略用例通过；完整 Service 包存在既有外部 OpenAI token 对比用例失败，未宣称全包通过 |
| 未完成项 | 未连接真实上游、Redis/PostgreSQL/OSS，未部署或重启生产；需在隔离环境用两个账号验证实际切换及第二账号失败后的终止行为 |
# 2026-09-04 图片账号连续失败熔断收尾

| 项目 | 记录 |
|---|---|
| 实际基线 | `main`；HEAD `314fcc3c0055a3be0c652782b646e71ad75df808`；`git describe --tags --always --dirty`=`v0.1.173.38-1-g314fcc3-dirty`；VERSION=`0.1.173.38` |
| 变更范围 | 新增图片账号 Redis 熔断器和运行时配置；接入 OpenAI/Gemini/Antigravity 图片候选过滤、sticky 校验、forward 结果反馈及管理端设置；补齐 `config.yaml`/环境变量回退字段映射；旧式 Gateway sticky 命中增加熔断复核，并增加同步/异步候选过滤测试。 |
| 验证结果 | 定向熔断、配置映射和调度过滤测试通过；`go test ./... -run '^$'`、`pnpm typecheck`、`pnpm build`、`git diff --check` 通过；构建保留既有 Browserslist、动态导入和大 chunk 警告。 |
| 未完成项 | 未执行真实 Redis/PostgreSQL/上游/OSS 链路、浏览器实机验收、生产部署或重启。 |

## 2026-09-04 生产异步生图只读诊断与本地复验

| 项目 | 记录 |
|---|---|
| 实际基线 | `git rev-parse HEAD`=`0ea8f5195ff64e6acecb3d131d68eac8553f203b`；`git status --short --branch`=`## main...origin/main [behind 1]`（文档为本轮修改）；`git describe --tags --always --dirty`=`v0.1.173.40-dirty`；VERSION=`0.1.173.38` |
| 生产只读范围 | `root@108.186.246.14` 的 systemd、PostgreSQL 任务快照和 `journalctl`；服务 active，生产 commit=`276f2240b80796a58cfb969a4c8b7f420a1310b9`，未修改数据库/配置/文件，未部署/重启 |
| 生产结论 | `invoking` 约 2-6；`account_id IS NULL=0`、租约超时=0、超过 10 分钟=0；更新时间持续刷新。错误是参考图 URL 上游 `curl (28)` 约 60 秒超时，不是未选账号 |
| 版本差异 | 生产 `0.1.173.38` 未包含本地 `v0.1.173.40` 的参考图 A/A/A/B 换号实现；生产同账号重复重试的主要原因是尚未发布本地修复 |
| 本地验证 | `go test ./internal/handler ./internal/repository` 通过；重试相关 `go test ./internal/service -run 'AsyncImage|Image.*Retry|Reference.*Retry' -count=1 -v` 通过；前端 `pnpm vitest run src/features/async-image-tasks/__tests__/api.spec.ts src/views/admin/__tests__/groupsImageAccountPools.spec.ts` 为 11/11 |
| 未完成项 | 完整 `go test ./internal/service -count=1` 仍被既有 `TestEstimateOpenAIInputTokens_CompareWithOpenAIAPI` 的外部网络超时阻断；未做真实上游/Redis/PostgreSQL/OSS 端到端、浏览器实机、生产发布或重启 |

## 2026-09-04 异步生图任务列表密度与操作按钮

| 项目 | 记录 |
|---|---|
| 实际基线 | `git rev-parse HEAD`=`0ea8f5195ff64e6acecb3d131d68eac8553f203b`；`git status --short --branch`=`## main...origin/main [behind 1]`（含本地未提交改动）；`git describe --tags --always --dirty`=`v0.1.173.40-dirty`；VERSION=`0.1.173.38` |
| 变更范围 | `frontend/src/components/common/DataTable.vue` 新增页面级 `compact` 横向内边距；`frontend/src/features/async-image-tasks/AsyncImageTasksView.vue` 管理端启用紧凑表格、最终账号列调整为 160px、列表手动结束操作改为仅 ban 图标并保留 tooltip/aria-label；补充 `DataTable` 紧凑布局回归测试 |
| 验证命令 | `cd frontend; pnpm vitest run src/components/common/__tests__/DataTable.spec.ts src/features/async-image-tasks/__tests__/api.spec.ts src/views/admin/__tests__/groupsImageAccountPools.spec.ts`；`pnpm test:run`；`pnpm typecheck`；`pnpm eslint src/components/common/DataTable.vue src/features/async-image-tasks/AsyncImageTasksView.vue`；`pnpm build`；`git diff --check` |
| 验证结果 | 定向测试 24/24；前端全量 239/239 文件、1623/1623 用例；类型检查、目标 ESLint、构建和 diff 检查通过。构建保留既有 Browserslist、动态导入和大 chunk 警告 |
| 未完成项 | 未执行浏览器实机、真实上游/Redis/PostgreSQL/OSS 端到端、生产部署、重启或灰度验收；后端完整 Service 包仍有既有外部 OpenAI token 对比测试网络超时边界 |

## 2026-09-04 分组清晰度账号池改为单列

| 项目 | 记录 |
|---|---|
| 实际基线 | `git rev-parse HEAD`=`0ea8f5195ff64e6acecb3d131d68eac8553f203b`；`git status --short --branch`=`## main...origin/main [behind 1]`（含本地未提交改动）；`git describe --tags --always --dirty`=`v0.1.173.40-dirty`；VERSION=`0.1.173.38` |
| 变更范围 | `frontend/src/views/admin/GroupsView.vue` 将图片账号池编辑区域从 `md:grid-cols-3` 改为固定单列 `grid-cols-1`，使 1K/2K/4K 每行显示一个；未改动输入解析、优先级和 API 保存逻辑 |
| 验证命令 | `cd frontend; pnpm vitest run src/views/admin/__tests__/groupsImageAccountPools.spec.ts src/views/admin/__tests__/groupsImagePricing.spec.ts src/views/admin/__tests__/groupsAsyncImage.spec.ts`；`pnpm typecheck`；`pnpm eslint src/views/admin/GroupsView.vue`；`git diff --check` |
| 验证结果 | 3 个测试文件、14/14 用例通过；类型检查、目标 ESLint 和 diff 检查通过 |
| 未完成项 | 未执行浏览器实机、真实上游/Redis/PostgreSQL/OSS 端到端、生产部署、重启或灰度验收 |

## 2026-09-16 OVH Hysteria2 故障绕行与 TCP 节点部署

| 项目 | 记录 |
|---|---|
| 实际 Git 状态 | `git status --short --branch` 为 `## main...origin/main` 加 `agent.md`、`readmenew.md`、`开发台账.md` 三份文档修改；完整 HEAD `52b0cf537f2e091506d52d72692195f6720088d6`；`git describe --tags --always --dirty`=`v0.1.173.49-dirty`；VERSION=`0.1.173.48` |
| 生产授权与范围 | 用户明确要求优化 `40.160.139.185`；安装官方 sing-box `1.14.1`，新增 VLESS + REALITY TCP `8443`、UFW `8443/tcp` 和独立 systemd 服务；更新 `0.0.0.0_9900/hy2.yaml`；未改 Nginx `443`，未停用原 Hysteria2 |
| 诊断结论 | Hysteria2 只能使用 UDP/QUIC；OVH 官方 Edge Network Firewall 文档说明 QUIC 会在网络边缘被丢弃，VAC 也可能自动介入。该机制可解释 UDP 线路的握手后无流量/断线，但是否正在强制清洗仍需 OVH 控制台 `Mitigation` 状态佐证 |
| 性能调整 | 系统已有 `net.ipv4.tcp_congestion_control=bbr`、`net.core.default_qdisc=fq`，运行网卡却仍为 `pfifo_fast`；执行 `tc qdisc replace dev ens3 root fq` 后运行态为 `fq`，未重启服务器 |
| 已执行验证 | `sing-box check -c .../reality.json` 通过；`systemd-analyze verify` 无目标单元错误；`sing-box-reality` enabled/active，TCP `0.0.0.0:8443` 监听；外部 `Test-NetConnection` 成功；UFW TCP/UDP `8443` 均允许；公开订阅与部署文件 SHA-256 一致，Mihomo `-t` 解析成功；真实 Mihomo 代理出口为 `40.160.139.185`，连续 5 次 HTTPS 成功 |
| 实测结果 | Cloudflare 10 MB 单连接约 `263295 B/s`；4 路并发 20 MB 切换 `fq` 前约 `655277 B/s`、后约 `695299 B/s`。结果证明 TCP 转发生效，不代表手机 5G、其他时段或其他运营商可达到相同速度 |
| 回退与边界 | 备份目录 `/root/hysteria-backups/20260916-ovh-tcp-reality/`；原 `hysteria-server.service`、`hysteria-server-8443.service` 均保持 active。未重启、未测试用户手机、未查看 OVH 控制台、未部署本仓库业务代码；直连中美线路吞吐仍明显低于服务器机房带宽 |

## 2026-09-16 TCP 配置小幅优化与复测

| 项目 | 记录 |
|---|---|
| 实际基线 | 完整 HEAD `52b0cf537f2e091506d52d72692195f6720088d6`；`git status --short --branch` 为 `## main...origin/main` 加三份交接文档修改；`git describe --tags --always --dirty`=`v0.1.173.49-dirty`；VERSION=`0.1.173.48`；保留上一轮未提交文档 |
| 授权与范围 | 用户明确要求继续配置优化；生产主机 `40.160.139.185` 新增 `/etc/sysctl.d/99-zz-proxy-tcp-tuning.conf`，仅定向加载本轮参数，不运行全量 sysctl 加载，不重启服务/服务器，不改订阅、Reality、Nginx 或 UFW |
| 参数 | `net.core.rmem_max/wmem_max=33554432`；`tcp_wmem=4096 16384 33554432`；`tcp_mtu_probing=1`；`tcp_slow_start_after_idle=0`；BBR/fq 和既有 `tcp_rmem` 保持不变。参数影响全主机网络栈，缓冲只提高上限，不预分配 32 MiB |
| 诊断证据 | 本轮国内测试下载采样 RTT 约 280 ms、重传字节 264256、`rwnd_limited=33.2%`，自动发送缓冲当时未达到旧上限；fq dropped=0。不能声称旧 4 MiB 上限是该测试线路当前瓶颈 |
| 验证命令/结果 | `sysctl -p /etc/sysctl.d/99-zz-proxy-tcp-tuning.conf` 和参数回读成功；`sing-box check -c /etc/sing-box/reality.json` 成功；三个代理服务 active；订阅/Reality 配置 SHA-256 与备份一致；5 次 HTTPS 返回 204；fq 无队列丢包 |
| 下载复测 | 同源 10 MB：基线 60 秒超时，9381120 字节/156337 B/s；调整后一次 10 MB 完整下载为 279873 B/s、35.73 秒。第二次握手超时，5 MB 重试也在 45 秒超时；没有稳定提升证据，不能保证手机约 4 MB/s 翻倍 |
| 回退/未完成 | `/root/hysteria-backups/20260916-tcp-buffer-tune/` 保存旧 sysctl 与未改动的节点/订阅；把新增 sysctl 文件移出 `/etc/sysctl.d/` 后运行 `sysctl -p .../sysctl.before.conf` 可回退。本轮未做手机 5G 新配置验收、项目测试或业务发布 |
