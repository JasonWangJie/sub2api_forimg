# new-api 图片功能 Codex 开发任务书

> 将本文件和下列三份文件一起复制到需要开发的 new-api 项目根目录，再让 Codex 阅读本任务书并执行。四份文件自包含需求，不需要可访问原生产站点或原数据库。源实现基线：`defccfacba610450ac5f9fd06e0f54c2f25fbb7a`，2026-09-16。

## 1. 必须一起提供的文件

1. [newapi图片功能迁移开发总纲.md](newapi图片功能迁移开发总纲.md)
2. [newapi异步生图API兼容规范.md](newapi异步生图API兼容规范.md)
3. [newapi图片页面与图库广场功能规格.md](newapi图片页面与图库广场功能规格.md)
4. 本任务书。

可选参考源仓库 `F:/Code/Git/sub2api_forimg`。如果在其他电脑没有此路径，仍可依据四份开发包实现；不要把原项目的服务器、域名、配置、凭证、安装路径或旧测试结论移入目标。

## 2. 可直接交给 Codex 的任务指令

请在当前 new-api 网关仓库中完成图片功能二次开发。先读取根目录本任务书及三份配套文档，核对真实 Git SHA、工作树、所有适用 AGENTS.md、当前主/子module依赖、认证/权限、分组、渠道、图片计费、数据库迁移、任务轮询、React路由和公共组件。保留用户已有改动。文档中目标只读快照 SHA `69a50029819a26c53e6babd276d49cfe2f8880ad` 仅作结构参考，目标当前代码优先。

请真正实现完整后端与前端，按下方阶段持续推进。不要只交计划、静态页面、mock任务列表或对原Sub2API站点的转发；默认复用new-api已有同步OpenAI/Gemini渠道，新增本地主数据库事实来源的持久异步编排。

最终交付：完整中英文异步生图API文档页面；用户异步任务中心；管理员生图任务中心；能力驱动图片工作台；本机个人图库；经过显式投稿/审核/同步的图片广场。同时完成这些页面依赖的图片策略/Token双平台映射、渠道池、并发/熔断、存储、延期投稿审核、举报、对象引用/清理、可靠Worker和持久计费幂等。

公共路径、HTTP202/200、header、BB/SC/OpenAI body与601–613以API兼容规范为准；站内API前缀/分组字符串/React组件按目标结构适配并同步实际文档。原同步入口和已有插件/视频任务保持原有契约。当前个人图库是本机实时图，异步结果默认不写本机图库；不要把旧云图库描述当成最终产品。

实现跨SQLite/MySQL/PostgreSQL的模型、迁移、claim/锁、幂等、配额和查询。不要复制PostgreSQL JSONB/ILIKE/partial index/SKIP LOCKED SQL而不提供目标兼容实现。优先GORM与项目既有锁和JSONwrapper。受影响relaykit保持独立构建；React使用目标Bun/Rsbuild/TanStack/i18next和公共组件，不引Vue、另一套基础UI或第二个登录体系。

计费先完成目标适配：复用原价格/倍率/用量计算，针对新增异步链延后持久结算；task staging与不可变实际quota命令同事务，存储后按持久账本幂等应用、确认日志再成功。不能让原请求预扣和新Apply同时扣，不能依赖内存settled标志、后台改价后重新计算或失败后重新生成。已可能发出上游请求但结果未持久化必须标unknown，禁止自动重放。跨主/日志库使用可靠事件和幂等确认，不假设跨库事务。

合理实现选择可依据当前仓库自主决定并记录，缺少信息时先完成不依赖该信息的本地工作。对实际数据库环境、生产凭证、已存在用户改动等无法自行确定且影响正确性的情况明确说明需要什么。测试环境必须隔离，不能擅自使用原项目或目标配置里的外部生产地址。

不要部署、远程连接、重启生产、清理真实图片/任务/账单或发布网站，除非用户明确另行授权。本任务是本地实现与验证。遵循目标AGENTS对认证/安全、三数据库真实验收和交接记录的具体要求，不能因为环境缺口把PENDING写成通过。

## 3. 顺序与每阶段可审查结果

### 阶段A：接入与基础设施

核对并记录target baseline。确定新增AsyncImageTask或证明现有Task扩展能隔离存储/账务状态；明确图片分组策略、Token映射和权限资源。建立GORM模型/升级迁移、提交幂等、Outbox/版本CAS、ephemeral/durable存储及图片验证器。补当前禁用的产物存储实现或接入兼容的具体ObjectStore。先交付持久结算接口和钱包/订阅/Token/日志故障重试不双扣的验证。

可审查结果：表/约束/索引与数据库适配；存储PUT/读取/Delete与intent恢复；结算Prepare/Apply/账本的真实行为，未完成真实DB测试标PENDING。

### 阶段B：公共持久异步协议

实现全部固定提交/上传/查询路径（含统一query与SC别名）、全部OpenAI尺寸映射和原生参数、Gemini BB顺序与SC别名、提交原始字节幂等、SC两阶段admission/reservation/alias/墓碑和恢复。复用上游执行链，持久全部图片/规格、固定账单、上传清单、账务日志确认。Worker startup/recovery、lease token、DB心跳/墙钟超时、独立终态context、分类重试和601–613均落地。

可审查结果：合法真实协议请求/响应（上游可先用测试替身）、同请求只一task、跨Key404、故障矩阵；已验证与真实上游/Redis/OSS缺口分开记录。

### 阶段C：用户与管理员任务中心

实现站内范围隔离、分页/排序/本地日期时区、完整筛选集合统计、queued展示与筛选一致、成功URL门槛和stable view。React共用任务业务组件，管理员额外渠道/尝试/reference URL详情。实现resume后处理、CAS terminate、最多100项当前页ID快照批量结束与逐项反馈。

可审查结果：真实持久记录页面，无行点击详情；用户敏感字段无泄露；并发成功被批量操作跳过、late worker不能覆盖管理员终态。

### 阶段D：工作台与本机个人图库

能力服务生成capability_version、模型级参数和平台/协议，提交前复核。实时/异步/Grok能力按当前可执行链展示；生成原body快照/unknown确认、异步轮询和任务入口、禁止跨模式回退。实时Blob写本机按用户30天/100张/200MiB，异步仅预览。完成图库网格、侧栏、下载/复用提示、删除、本机配额/过期/object URL释放与投稿Blob保留。

可审查结果：管理员改能力后旧表单停止提交；网络未知相同body/key确认；个人图库与异步任务结果真值分离；多用户本机数据隔离。

### 阶段E：投稿、广场、治理与配置

元数据延期投稿→approved_pending_sync→校验原Blob→durable对象/资产/published/synced，阶段失败与并发幂等可收敛。已有资产pending_review投稿、审核/批量审核/拒绝/下架/恢复/撤回、prompt opt-in、公开content鉴权/短缓存、举报open去重与处理。维护Outbox/清理job/引用保护、真实旧数据才迁移。完善Token映射/图片策略/三模式渠道池和存储/并发/重试/配额/保留设置。

可审查结果：未审图不公开、share_prompt=false不泄露、hash不同sync拒绝、重复sync原结果、删除不误删其他引用；后台当前页批量结束与批量审核各自正确。

### 阶段F：站内API文档与完整验证

React中英文文档目录/端点卡片/参数表/合法可复制JSON/curl/受理与查询/601–613/时限。Base URL动态读取，文档与路由/DTO共用或验证契约。完成导航/权限/空态/错误/暗色/窄屏/键盘/弹窗焦点。运行目标规定测试、真实三数据库新建与升级/重跑矩阵、隔离浏览器与链路测试；更新运行/升级/配置/开发交接文档。

可审查结果：最终diff、实际命令/结果/SHA、浏览器证据、真实DB版本和未验证项。不能引用来源历史全量通过作为本次目标验证。

## 4. 禁止降级为“已完成”的情况

- 只完成页面或只生成随机task ID，没有task/event/outbox持久事实。
- task只有Redis数据，重启/清Redis即丢失。
- 上游成功但失败重试又生成，或unknown自动重跑。
- 图片先暴露URL再扣费，或staging/账单不能一起持久化。
- 账本只在内存，相同任务多次Apply或日志补写会多扣。
- 普通用户能读其他Key公共任务、管理员渠道/参考URL、未共享prompt。
- 个人图库偷偷变云图库、异步自动入本机、实时默认上传OSS。
- 延期投稿批准就展示公开，但用户尚未上传；sync不校验原hash。
- 清理直接删OSS而不检查task/library/publication引用。
- 站内指南仍使用旧SC pending/completed、code/data包装或固定24小时签名说明。
- 测试只有mock/编译，却宣称三数据库、多实例、真实上游或生产已经通过。

## 5. 默认排除的新增产品

视频、点赞/评论/收藏、个人图库跨设备云同步、新支付/订阅机制、重新设计原网关认证、把同步URL改异步、从原站批量搬生产数据均不在本次需求。源存储Superbed全面供应商兼容、历史多凭证resolver等以总纲标注的等价范围和扩展项交付状态为准，不能无说明遗漏或声称已存在。

## 6. 交付时说明

最终报告应让用户能判断：六项页面哪里进入、固定API是否全部兼容、配置如何启用、数据/计费/原接口边界、哪些测试实际通过、哪些真实环境仍PENDING。列出实际目标HEAD/分支/dirty状态、改动文件、验证命令与未完成项。完成前diff check、新增文档链接与复制示例检查，遵循目标AGENTS的记录同步要求。

本开发包当前只完成来源功能提取和目标结构只读适配，不是上述代码阶段的完成报告。不要在刚收到文档时声称功能已开发或可生产上线。
