# 发行版发布与安装操作手册

> 仓库：`JasonWangJie/sub2api_forimg`
> 配套：`deploy/FORK_RELEASE.md`

---

## 日常发版（就这几步）

先确保改动已在 `main` 并已推送，仓库为 Public，且 GitHub Actions 已启用。然后：

```bash
# 确认已推到 main
git checkout main
git pull origin main

# 打 annotated tag（Release 说明会进 GitHub Release）
git tag -a v0.1.173.38 -m "#**feat: 增强异步图片任务管理，新增管理员终止能力**

- 新增功能：允许管理员手动终止处于非终态的异步图片任务。
- 增加异常处理逻辑，针对无法安全关闭的任务终止操作做容错。
- 更新异步图片任务服务，新增管理员标记任务为失败的方法。
- 更新异步图片接口文档，补充错误处理与任务管理的变更说明。
- 优化任务失败场景下的错误码与返回响应，包含详细失败原因及分类。
"

# 推送 tag（这一步会触发 Actions 自动打包）
git push origin v0.1.173.38
```

下次发版只改版本号即可（见下方「版本号规则」）。

然后打开：

1. https://github.com/JasonWangJie/sub2api_forimg/actions → 等 **Release** 绿勾
2. https://github.com/JasonWangJie/sub2api_forimg/releases → 确认有对应 `sub2api_*_linux_amd64.tar.gz` / `arm64` / `checksums.txt`
3. https://github.com/JasonWangJie/sub2api_forimg/pkgs/container/sub2api_forimg → 确认 GHCR 有同版本镜像

> CI 会按 tag 自动写入 `VERSION` 并上传产物，**不必**先手改 VERSION 再发版。
> Release 工作流使用当前仓库的 `GITHUB_TOKEN`，GoReleaser 的 owner/name 取当前仓库上下文，因此 GitHub Release 和 GHCR 都发布到 `JasonWangJie/sub2api_forimg`。
> 第一次生成 GHCR 包后，在 Package settings → Change visibility 中设为 **Public**；否则未登录服务器执行 `docker pull ghcr.io/jasonwangjie/sub2api_forimg:...` 会被拒绝。

### 若页面像「只有源码 zip」——说明还没打出 Release

| 你看到的 | 含义 |
|---|---|
| Tags 页 + 只有源码 zip + 「Create release from tag」 | 只推了 tag，**Actions 没跑完或没跑** |
| Releases 页 + `github-actions` + 多个 `sub2api_*.tar.gz` | 打包成功 |

补跑：Actions → **Release** → **Run workflow** → 填 tag → **不要勾** simple_release。

---

## 版本号规则（支持四段）

`install.sh` / `upgrade.sh` 支持：

- 三段（跟上游）：`v0.1.162`
- 四段（本 Fork 中途更新）：`v0.1.162.1`

推荐节奏：

| 时机 | 示例 tag |
|---|---|
| 对齐原作者 `0.1.162` | `v0.1.162` |
| 自己又改了东西 | `v0.1.162.1`、`v0.1.162.2` … |
| 原作者到了 `0.1.163`，合并后再发 | `v0.1.163` |
| 合并后自己又改 | `v0.1.163.1` |

注意：

- tag 必须带 `v`；**不要**重复推已存在的同名 tag  
- 已发布的版本有问题才删 tag/Release 重推；正常更新请升号  
- 推新 tag 会自动触发 Actions，不必每次手动 Run workflow  

---

## 产物出来后怎么装

```bash
# 安装
curl -sSL https://raw.githubusercontent.com/JasonWangJie/sub2api_forimg/main/deploy/install.sh | sudo bash

# 升级（会升到本仓库 releases/latest）
curl -sSL https://raw.githubusercontent.com/JasonWangJie/sub2api_forimg/main/deploy/upgrade.sh | sudo bash
```

Docker 安装或升级使用当前 Fork 的 GHCR 镜像：

```bash
docker pull ghcr.io/jasonwangjie/sub2api_forimg:latest
```

目标机：Linux + PostgreSQL + Redis + sudo。

```bash
sudo systemctl status sub2api
/opt/sub2api/sub2api --version
# 配置：/opt/sub2api/config.yaml
```

---

## 合并原作者后

1. 合并 `upstream`，按 `deploy/FORK_RELEASE.md` 保住 Fork 身份  
2. 用上游版本号打三段 tag（如 `v0.1.163`）  
3. 之后自研再用四段（`v0.1.163.1`）  

---

## 常见问题

| 现象 | 处理 |
|---|---|
| Actions 要 Enable | 先启用再推 tag |
| `startup_failure` | 多为 workflow `if` 写了 `secrets.*`（已修） |
| 没有 `.tar.gz` | 不要勾选 simple_release |
| install 拉不到版本 | Release 还没跑完 |
| 本机生图 URL 404 / 仍是旧 CDN | 见下方「本机图片访问」；完整说明 [wiki-new/对象存储与保留策略.md](wiki-new/对象存储与保留策略.md) |

### 本机图片访问（安装后）

异步结果存本机目录时，访问方式二选一：

1. **签名下载（默认）**：管理端留空 `local_url`，填写与网站一致的「站点/API 公开地址」（建议「任务查询」公开地址相同）→ `https://站点/v1/images/local/...?exp=&sig=`。管理端已保存后以界面为准，不受 `config.yaml` 里旧 `async_image.public_base_url` 覆盖。
2. **专用图片域名**：宝塔新建站点，网站目录 = `/opt/sub2api/data/image_storage`，后台 `local_url` = `https://图片域名` → `https://图片域名/images/results/...`；若 403：`chmod -R a+rX /opt/sub2api/data/image_storage`

- Actions：https://github.com/JasonWangJie/sub2api_forimg/actions
- Releases：https://github.com/JasonWangJie/sub2api_forimg/releases
- GHCR：https://github.com/JasonWangJie/sub2api_forimg/pkgs/container/sub2api_forimg
